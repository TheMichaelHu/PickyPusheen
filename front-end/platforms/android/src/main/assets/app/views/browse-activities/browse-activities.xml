<Page loaded="loaded">
    <GridLayout>
        <ListView items="{{ activityList }}" itemTap="selectActivity" row="1" colspan="2">
            <ListView.itemTemplate>
                <Label text="{{ activity }}" horizontalAlignment="left" verticalAlignment="center"/>
            </ListView.itemTemplate>
        </ListView>
    </GridLayout>
</Page>
