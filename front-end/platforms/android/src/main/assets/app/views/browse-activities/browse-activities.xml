<Page loaded="loaded">
    <GridLayout>
        <ListView id="activityList" items="{{ activityList }}" row="1" colspan="2">
            <ListView.itemTemplate>
                <Label id="labelActivity" text="{{ title }}" horizontalAlignment="left" verticalAlignment="center"/>
            </ListView.itemTemplate>
        </ListView>
    </GridLayout>
</Page>
