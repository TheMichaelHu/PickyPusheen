<Page loaded="loaded">
    <GridLayout>
        <ListView items="{{ activityList }}" itemTap="selectActivity">
            <ListView.itemTemplate>
                <Label text="{{ activity }}" horizontalAlignment="left" verticalAlignment="center"/>
            </ListView.itemTemplate>
        </ListView>
    </GridLayout>
</Page>
